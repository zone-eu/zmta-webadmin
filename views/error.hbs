<h2>{{message}}</h2>

<p>{{error.status}}</p>
{{#if error.errorStack}}
    <pre>{{error.errorStack}}</pre>
{{/if}}

{{#if error.logEntries}}
    <h3>Log entries for {{logId}}</h3>

    <div class="table-responsive">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Date
                    </th>
                    <th>
                        ID
                    </th>
                    <th>
                        Action
                    </th>
                    <th>
                        Data
                    </th>
                </tr>
            </thead>
            <tbody>
                {{#each error.logEntries}}
                    <tr>
                        <td class="col-md-1">
                            <code>{{time}}</code>
                        </td>
                        <td>
                            <code>{{id}}</code>
                        </td>
                        <td>
                            <code>{{action}}</code>
                        </td>
                        <td>
                            <table class="table">
                                {{#each message}}
                                    <tr>
                                        <td>
                                            <code>{{key}}</code>
                                        </td>
                                        <td>
                                            <code>{{value}}</code>
                                        </td>
                                    </tr>
                                {{/each}}
                            </table>
                        </td>
                    </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

{{/if}}
